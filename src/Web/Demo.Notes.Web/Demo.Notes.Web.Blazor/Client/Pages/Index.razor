@page "/"

<PageTitle>Home</PageTitle>

<h1>Administration for Notes data</h1>

<AuthorizeView>
    <Authorized>
        <h3>Claims for logged in User:</h3>
        <dl>
            @foreach (var claim in @context.User.Claims)
            {
                <dt>@claim.Type</dt>
                <dd>@claim.Value</dd>
            }
        </dl>
    </Authorized>
</AuthorizeView>